# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

#rest style urls : http://stackoverflow.com/questions/2001773/understanding-rest-verbs-error-codes-and-authentication
#test rest style urls : http://blogs.plexibus.com/2009/01/15/rest-esting-with-curl/

# Home page
GET     /                           controllers.NotebookController.index()

# Authentication
GET     /login                      controllers.Application.login()
POST    /login                      controllers.Application.authenticate()
GET     /logout                     controllers.Application.logout()

#Notebooks
GET     /notebooks                  controllers.NotebookController.list()
POST	/notebooks					controllers.NotebookController.create()
DELETE  /notebooks/:id       		controllers.NotebookController.delete(id:String)
PUT     /notebooks/:id     			controllers.NotebookController.update(id:String)

#Notes
GET     /notebooks/:notebookId/notes     	controllers.NoteController.list(notebookId:String)
GET     /notebooks/:notebookId/notes/:id   	controllers.NoteController.html(id:String, notebookId:String)
POST    /notebooks/:notebookId/notes       	controllers.NoteController.create(notebookId:String)
PUT     /notebooks/:notebookId/notes/:id    controllers.NoteController.update(id:String, notebookId:String)
DELETE  /notebooks/:notebookId/notes/:id    controllers.NoteController.delete(id:String, notebookId:String)

# Javascript routing
GET     /assets/javascripts/routes  controllers.Application.javascriptRoutes()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

#temp to create notebooks
#GET	/notebooks/create/:title		controllers.NotebookController.create(title:String)
#GET	/notebooks/:id/:note			controllers.NoteController.create(id:String, note:String)